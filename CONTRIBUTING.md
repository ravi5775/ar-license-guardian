# Contributing

## Generated route tree

Typecheck and development-server runs may regenerate `src/routeTree.gen.ts` with
cosmetic import-order changes. Review `git status` before committing and exclude
unrelated generated churn from focused changes.
